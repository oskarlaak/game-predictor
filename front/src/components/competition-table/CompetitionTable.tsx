import ICompetitionTableDTO from "../../dto/competition/ICompetitionTableDTO";

type Props = {
    c: ICompetitionTableDTO;
};

export default function CompetitionTable({c}: Props): JSX.Element {
    return <>
        Table
    </>;
}
